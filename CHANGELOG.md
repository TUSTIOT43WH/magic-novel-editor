# 更新说明

## 2026-09-23

### 图片与文字共同创作

- Vibe 输入框下方新增图片上传、预览和移除功能，支持最多 4 张 PNG/JPEG/WebP 图片，每张不超过 5 MB。
- 点击生成后，图片、文字描述和选中的上下文一同发送给作者配置的模型，正文保持流式输出。
- 图片按书籍和章节分别暂存，不写入小说数据库；刷新页面后需要重新选择。
- 对图片数量、大小、格式和读取失败提供提示；模型报错时保留图片以便重试。
- 同时适配 Chat Completions 和 Responses 的图片消息格式；必须使用支持图片输入的模型。

### 章节总结与时间线

- 新增“AI 优化总结”按钮，发送本章完整正文、章节信息及专用总结提示词，生成关键事件、人物关系变化、章末状态和伏笔总结。
- AI 总结会保存并锁定，同时更新本章已有的自动时间线摘要；后续召回支持读取新版 AI 总结。
- 时间线按章节顺序展示，避免自定义历法名称、年号变化和月份文字排序导致后续章节跑到列表前面。
- 自定义时间完整显示，长时间线保留独立滚动区域。

### clean 版本与验证

- 保留空白初始化，不包含原版的内置示例小说、个人书稿、图片附件、API Key 或运行时数据库。
- 未配置模型及模型空响应继续返回明确错误，不回退到演示正文。
- 新增无需真实 API Key 的图文接口回归测试：运行 `node --test tests/story-images.test.mjs`。

## 2026-09-03

### 写作与长篇上下文

- 章节侧栏支持独立滚动，长篇作品可以继续查看第 11 章之后的章节。
- 召回上限由 12 条提高到 18 条，并减少同一章节总结与时间线的重复召回。
- 章节召回总结改为结构化记录时间、地点、主要角色、人物关系、关键事件、章末状态和未解决事项。
- 召回候选支持作者手动加入、移除和批量管理。
- 知识库长文本默认折叠，可按需展开全文。

### 人物与关系

- 人物状态改为作者手动维护，避免自动识别把普通词语误当成人物。
- 人物支持颜色与登场状态标记。
- 新增人物关系网络，可查看关系名称、强度和关联角色信息。
- 人物与关系支持批量删除。

### 连贯性审校与作者助手

- 新增并行智能体工作台，包括写作、连贯性审校和作者研究助手。
- 快速审校会读取当前章节之前最近十章的完整正文，并通过单次模型请求检查跨章逻辑。
- 审校报告支持流式显示；连接中断时保留已生成内容，无输出时在报告区显示具体错误。
- DeepSeek Chat Completions 审校请求自动关闭思考模式，避免思考过程耗尽输出额度后没有最终报告。
- 支持上传 TXT 进行可选的全书深读，并在发送前明确提示 Token 消耗。
- 作者助手支持可选的本书 RAG，可查询已有剧情、人物和设定。
- 写作台右下角新增可折叠悬浮助手，可以边查看审校建议或问答结果，边修改正文。

### 模型与隐私

- 模型连接可以保存在本机浏览器中，重新打开本地页面无需重复填写。
- 支持 OpenAI 兼容的 Chat Completions 与 Responses 接口，以及流式正文输出。
- clean 版本不包含示例小说、用户书稿、人物、关系、知识库、API Key 或本地数据库数据。
- 未配置模型连接时会返回明确提示，不再输出内置演示小说。
